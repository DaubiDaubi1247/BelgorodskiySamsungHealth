import * as React from 'react';

interface IMainProps {
}

const Main: React.FunctionComponent<IMainProps> = (props) => {
  return (
    <div>
        {/* мб все тренировки??? */}
    </div>
  );
};

export default Main;
